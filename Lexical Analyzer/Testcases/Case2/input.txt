/* This is a multiline comment \t \n
#include <stdio.h>\n "tsr'tss
*/

// Function declaration
void myFunction();
	double globalVariable=1000.2484e-7;
	double globalVariable2=.32e-7;

int main() {
	// Single-line comment\
check kortesi
	//this is another Single line comment\    
	double dblConstant = .32e-2;
	int integerConstant = 10;
	float floatConstant = 3.14;
	//this is another Single line comment\
	char charConstant = 'A';
	char stringConstant[] = "Hello, World!\
Testing flex\
Ki ashe output
";

	// If-else statement
	if (integerConstant > 5) {
		printf("Integer is greater than 5.\n");
	} else {
		printf("Integer is less than or equal to 5.\n");
	}

	// For loop
	for (int i = 0; i < 5; i++) {
		printf("Iteration %d\n", i);
	}

	// While loop
	int counter = 0;
	while (counter < 3) {
		printf("Counter value: %d\n", counter);
		counter++;
	}

	// Switch statement
	switch (charConstant) {
		case 'A':
			printf("Char constant is A.\n");
			break;
		case 'B':
			printf("Char constant is B.\n");
			break;
		default:
			printf("Char constant is not A or B.\n");
	}

	// Function call
	myFunction();

	return 0;
}

// Function definition
void myFunction() {
	printf("Inside myFunction()\n");
}
char str[100]="I have a \" test\" function.so i\
asked \
\"how to test\"";
//checking for comment /* nested \
test*/ comment
//checking for comment /* nested
dhhds scguefhhgdfs sfdgdfshudgfes
test*/ comment

12.1ef
12.efg
12abcd
'\n
..
1E.2113
1e1.2113
'abcd 
1.2.3E1.2
''A 
..
...
''' 
'\?'
'\x'
'\f'
..'..